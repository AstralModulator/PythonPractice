#You can use your own files as modules or you can use pythons built in library.
import circle

radius = float(input("Enter the radius of the circle in cm: "))
print(circle.area(radius))
print(circle.circumference(radius))